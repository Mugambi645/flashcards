# docker-compose.yml
services:
  web:
    build: .
    volumes:
      - .:/app
      - sqlite_data:/app/db
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
    restart: unless-stopped

volumes:
  sqlite_data: